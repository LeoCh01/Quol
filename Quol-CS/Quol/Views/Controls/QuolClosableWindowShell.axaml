<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Quol.Views.Controls.QuolClosableWindowShell"
             x:Name="Root">

  <Border Background="{StaticResource QuolContentBrush}"
          CornerRadius="6"
          ClipToBounds="True">
    <Grid Margin="0" RowSpacing="0" ColumnSpacing="0" RowDefinitions="Auto,*">
      <Border x:Name="TitleBarHost"
              Grid.Row="0"
              Background="{StaticResource QuolTitleBarBrush}"
              BorderThickness="0,0,0,1"
              PointerPressed="OnTitleBarPointerPressed">
        <Grid ColumnDefinitions="*,Auto" Margin="12,0">
          <TextBlock Text="{Binding WindowTitle, ElementName=Root}"
                     IsHitTestVisible="False"
                     Margin="0,12"
                     HorizontalAlignment="Left"
                     TextAlignment="Left"
                     VerticalAlignment="Center"
                     FontSize="12"
                     FontWeight="Bold" />

          <Button Grid.Column="1"
                  Classes="icon titlebar-icon"
                  Margin="6,6,0,6"
                  VerticalAlignment="Center"
                  ToolTip.Tip="Close"
                  Click="OnCloseClicked"
                  Content="✕" />
        </Grid>
      </Border>

      <Border Grid.Row="1"
              Background="{StaticResource QuolContentBrush}">
        <ContentPresenter Content="{Binding InnerContent, ElementName=Root}" />
      </Border>
    </Grid>
  </Border>
</UserControl>
